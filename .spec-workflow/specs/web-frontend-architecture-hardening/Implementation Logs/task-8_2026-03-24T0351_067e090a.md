# Implementation Log: Task 8

**Summary:** 建立统一通知与错误处理基础设施，新增全局通知中心与错误归一化工具，并替换登录、登出、候选人详情、LUI 模块中的 `alert`、空 catch、`.catch(() => undefined)` 和零散错误输出模式。

**Timestamp:** 2026-03-24T03:51:43.177Z
**Log ID:** 067e090a-9273-44ff-b5fa-82a7663f93a4

---

## Statistics

- **Lines Added:** +311
- **Lines Removed:** -8
- **Files Changed:** 14
- **Net Change:** 303

## Files Modified
- apps/web/src/App.vue
- apps/web/src/components/app-user-actions.vue
- apps/web/src/components/lui/agent-selector.vue
- apps/web/src/components/lui/candidate-selector.vue
- apps/web/src/stores/auth.ts
- apps/web/src/stores/lui/conversations.ts
- apps/web/src/stores/lui/messages.ts
- apps/web/src/stores/lui/files.ts
- apps/web/src/views/CandidateDetailView.vue
- apps/web/src/views/LoginView.vue
- .spec-workflow/specs/web-frontend-architecture-hardening/tasks.md

## Files Created
- apps/web/src/lib/errors/normalize.ts
- apps/web/src/composables/use-app-notifications.ts
- apps/web/src/components/app-notification-center.vue

---

## Artifacts

### Components

#### AppNotificationCenter
- **Type:** Vue
- **Purpose:** 在应用根节点统一渲染全局通知列表，负责展示成功/错误/警告/信息提示并支持手动关闭
- **Location:** apps/web/src/components/app-notification-center.vue
- **Props:** None (reads notification state from useAppNotifications())
- **Exports:** AppNotificationCenter (default)

### Functions

#### normalizeAppError
- **Purpose:** 将 ApiError、Error、字符串和未知异常统一归一化为可展示的错误详情对象
- **Location:** apps/web/src/lib/errors/normalize.ts:18
- **Signature:** (error: unknown, options?: { title?: string; fallbackMessage?: string }) => AppErrorDetails
- **Exported:** Yes

#### reportAppError
- **Purpose:** 统一记录带作用域的错误日志，并返回归一化后的错误详情供通知/状态层复用
- **Location:** apps/web/src/lib/errors/normalize.ts:58
- **Signature:** (scope: string, error: unknown, options?: { title?: string; fallbackMessage?: string }) => AppErrorDetails
- **Exported:** Yes

#### isUnauthorizedError
- **Purpose:** 识别鉴权状态检查中的 401 错误，避免把正常未登录态误报给用户
- **Location:** apps/web/src/lib/errors/normalize.ts:68
- **Signature:** (error: unknown) => boolean
- **Exported:** Yes

#### useAppNotifications
- **Purpose:** 提供全局通知读写接口，封装通知创建、自动消失、手动关闭与错误通知能力
- **Location:** apps/web/src/composables/use-app-notifications.ts:99
- **Signature:** () => { notifications: Readonly<Ref<AppNotification[]>>; notify: (message: string, options?: NotifyOptions) => string; notifySuccess: (...); notifyInfo: (...); notifyWarning: (...); notifyError: (error: unknown, options?: NotifyErrorOptions) => AppErrorDetails; dismiss: (id: string) => void; clear: () => void }
- **Exported:** Yes

### Integrations

#### Integration
- **Description:** 应用根组件挂载全局通知中心，让任意 composable、store、页面都能通过共享通知状态显示用户反馈
- **Frontend Component:** App.vue + AppNotificationCenter
- **Backend Endpoint:** None
- **Data Flow:** 页面/组件/store 捕获错误 → reportAppError 归一化并记录作用域日志 → useAppNotifications 写入通知队列 → AppNotificationCenter 渲染提示并自动消失

#### Integration
- **Description:** 登录页将二维码加载失败和登录状态检查失败统一接入错误归一化与通知机制，同时保留页面内状态展示
- **Frontend Component:** LoginView
- **Backend Endpoint:** GET /api/auth/baobao/qr, GET /api/auth/baobao/login-status
- **Data Flow:** authApi 请求失败 → reportAppError 记录与标准化 → useAppNotifications.notifyError 展示全局提示 → 页面局部状态继续控制卡片内文案

#### Integration
- **Description:** LUI store 模块把会话加载、创建、删除、发送消息、文件上传删除等异常统一映射为全局通知，而非仅 console 输出
- **Frontend Component:** stores/lui/conversations.ts, stores/lui/messages.ts, stores/lui/files.ts
- **Backend Endpoint:** LUI conversation/message/file endpoints
- **Data Flow:** luiApi 调用失败 → store 维护 error ref → reportAppError 记录作用域日志 → useAppNotifications.notifyError 给出一致的全局用户反馈

#### Integration
- **Description:** 候选人详情页和用户菜单把工作台启动、登出、跳转失败等动作统一接入通知层，替代 alert 和吞错写法
- **Frontend Component:** CandidateDetailView + AppUserActions
- **Backend Endpoint:** POST/GET opencode workspace and auth logout endpoints
- **Data Flow:** 用户触发动作 → API/路由失败时归一化错误 → 全局通知展示失败原因；成功时通过 notifySuccess 告知结果


# Implementation Log: Task 2

**Summary:** 为 LUI 建立独立流式消息协议层，新增健壮的 SSE parser 与 `streamMessage` API，将原本散落在 store 中的 `ReadableStream` 读取、chunk 拼接和事件解析迁移到 `api/core/stream.ts`。

**Timestamp:** 2026-03-24T03:16:22.928Z
**Log ID:** 51e6af96-a33d-433e-9638-fe61c42c0352

---

## Statistics

- **Lines Added:** +241
- **Lines Removed:** -85
- **Files Changed:** 4
- **Net Change:** 156

## Files Modified
- apps/web/src/api/client.ts
- apps/web/src/api/lui.ts
- apps/web/src/stores/lui.ts

## Files Created
- apps/web/src/api/core/stream.ts

---

## Artifacts

### Functions

#### consumeLuiMessageStream
- **Purpose:** 解析后端 UI message SSE 流，处理跨 chunk 拼接、事件边界识别、错误事件和最终状态汇总
- **Location:** apps/web/src/api/core/stream.ts:112
- **Signature:** (response: Response, callbacks?: LuiStreamCallbacks) => Promise<LuiStreamMessageState>
- **Exported:** Yes

#### streamMessage
- **Purpose:** 为 LUI 会话消息发送提供统一流式接口，内部发起请求并委托协议层解析更新
- **Location:** apps/web/src/api/lui.ts:52
- **Signature:** (conversationId: string, input: SendMessageInput, options?: LuiStreamCallbacks & { signal?: AbortSignal }) => Promise<LuiStreamMessageState>
- **Exported:** No

### Integrations

#### Integration
- **Description:** LUI store 通过 `luiApi.streamMessage` 订阅协议层回调，不再直接读取 `Response.body` 或自行解析 SSE。
- **Frontend Component:** useLuiStore
- **Backend Endpoint:** POST /api/lui/conversations/:id/messages
- **Data Flow:** store 创建占位 assistant message → `luiApi.streamMessage` 发起请求 → `consumeLuiMessageStream` 解析 SSE 事件并回调更新 → store 仅同步 message 状态与内容


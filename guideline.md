# IMS 设计语言与组件规范

IMS 是桌面候选人管理工具，不是营销站，也不是低密度 SaaS 首页。界面应像一张稳定的工作台：信息清楚、操作靠近上下文、层次克制但不贫乏。

本规范是 UI 设计和前端实现的共同依据。新增页面、重构组件、改设计稿时优先按本文执行。

---

## 1. 设计原则

### 1.1 桌面工作台优先

- 信息密度高于装饰性留白。
- 关键操作始终可见，避免把主操作藏到悬浮或滚动末端。
- 页面结构稳定：左导航、顶部上下文、主内容、侧边辅助信息。
- 数据列表、表格、筛选、导入、AI 初筛都应服务于快速判断和批量处理。

### 1.2 层次来自背景、间距和内容组织

- 默认不用重阴影，不用厚边框。
- 用浅蓝灰画布承托白色模块。
- 用模块内部节奏区分标题、摘要、表单、操作、状态。
- 可使用轻量卡片，但不要把所有内容都套成同质卡片网格。
- 当区域显得空而轻时，优先补环境层次，不优先补更多卡片或说明文。
- 环境层次可来自极淡的顶部光带、柔和光斑、低对比网格、单根分隔气氛线。
- 纹理必须退后到信息之后，只负责提升质感，不能影响可读性和扫描速度。

### 1.3 克制但有明确性格

- 主色是冷静的钴蓝，强调 AI、导入、筛选等核心动作。
- 辅色只用于语义分区，例如成功、警告、橙色经验段、绿色技能段。
- 不做大面积渐变、玻璃拟态、彩色侧边条、营销页 Hero。
- 高级感来自秩序、比例和材质，不来自更多装饰元素。
- 少量浅蓝纹理、半透明白层、内高光可以使用，但必须非常克制，只能作为桌面工作台的环境质感。
- 说明文字默认先删一半，再判断是否还需要保留。

---

## 2. 色彩系统

### 2.1 核心色

| Token | 色值 | 用途 |
|---|---:|---|
| `ims-accent-cobalt` | `#0062FF` | 主操作、链接、当前步骤、AI 重点信息 |
| `ims-accent-cobalt-strong` | `#1D4ED8` | 高强调数字、拖拽控件段色 |
| `ims-accent-cobalt-soft` | `#EEF4FF` | 激活浅底、提示块、选中态背景 |
| `ims-accent-cobalt-muted` | `#CFE0FF` | 弱边界、胶囊边框 |

### 2.2 中性色

| Token | 色值 | 用途 |
|---|---:|---|
| `ims-text-primary` | `#1A1A1A` | 标题、关键数值、主文本 |
| `ims-text-secondary` | `#4B5563` | 描述、说明、次级元信息 |
| `ims-text-muted` | `#6B7280` | 辅助标签、时间、提示 |
| `ims-border-subtle` | `#E5E7EB` | 极弱分割线、必要边框 |
| `ims-border-bluegrey` | `#E0E9F3` | 桌面外壳边界 |
| `ims-page-bg` | `#F3F6FA` | 页面画布 |
| `ims-shell-bg` | `#F8FAFD` | 工作台外壳、弹窗画布 |
| `ims-panel-bg` | `#FFFFFF` | 模块、卡片、表单容器 |
| `ims-panel-muted` | `#F9FAFB` | 次级内容块 |

### 2.3 语义色

| 场景 | 主色 | 浅底 | 用途 |
|---|---:|---:|---|
| 成功 / 技能 | `#10B981` | `#ECFDF5` | 通过、技能匹配、可用状态 |
| 警告 / 待定 | `#F59E0B` | `#FFFBEB` | 待复核、风险提示 |
| 经验 / 项目 | `#F97316` | `#FFF7ED` | 项目经历权重、经验信号 |
| 危险 / 淘汰 | `#EF4444` | `#FEF2F2` | 删除、失败、淘汰 |

### 2.4 使用边界

- 主色不要铺满整屏，优先用于操作、当前状态和重要数据。
- 状态色必须绑定语义，不作纯装饰。
- 不使用纯黑 `#000000`；正文统一 `#1A1A1A`。
- 白色模块可以继续使用 `#FFFFFF`，但页面画布不使用纯白平铺。

---

## 3. 排版

| 场景 | 字号 | 字重 | 行高 | 用途 |
|---|---:|---:|---:|---|
| 页面标题 | 20px | 600 | 1.15 | 候选人、导入工作台、设置页标题 |
| 弹窗标题 | 18px | 600 | 1.2 | 表单弹窗、确认弹窗 |
| 区块标题 | 16px | 600 | 1.25 | 卡片 / 面板标题 |
| 小标题 | 14px | 600 | 1.35 | 表单分组、列表行主信息 |
| 正文 | 14px | 400 | 1.4 | 常规内容 |
| 辅助说明 | 12px | 400 | 1.45 | 提示、元信息、说明文字 |
| 标签 / 计数 | 12px | 600 | 1.2 | Badge、Tab 计数、状态 |

文字规则：

- 标题要短，避免重复说明。
- 说明文字只解释下一步或影响，不复述标题。
- 操作按钮用动词开头，例如“开始导入”“保存模板”。
- 不使用夸张营销语，不使用空泛的“智能赋能”。

---

## 4. 间距、圆角、阴影

### 4.1 间距

| 层级 | 推荐值 |
|---|---:|
| 页面水平边距 | 48px / 64px |
| 页面垂直边距 | 16px / 24px |
| 主区域 gap | 16px / 20px |
| 面板内边距 | 14px / 16px / 18px |
| 表单项 gap | 8px / 12px |
| 列表行高度 | 64px / 74px |

### 4.2 圆角

| 元素 | 圆角 |
|---|---:|
| 小按钮、输入框、Badge | 6px |
| 普通面板、列表项 | 6px / 8px |
| 桌面外壳、大弹窗 | 16px / 20px |
| 顶部环境层 | 22px / 24px |

### 4.3 阴影

- 常规卡片、列表项、输入框：无阴影。
- 弹窗外层：允许极轻环境阴影。
- 下拉浮层：允许投影建立浮起关系。
- 禁止为每张卡片添加默认阴影。

推荐浮层阴影：

```text
color: rgba(15, 23, 42, 0.35)
blur: 32px
y: 14px
spread: -18px
```

---

## 5. 页面结构

### 5.1 标准页面

```text
页面画布 #F3F6FA
└─ 桌面外壳 #F8FAFD / 轻边界 / 16-22px 圆角
   ├─ 顶部标题区：标题、说明、主操作
   ├─ 筛选 / 状态区：搜索、Tab、批量动作
   └─ 主内容区：列表 / 表格 / 工作台 / 详情
```

### 5.2 工作台页面

适用于导入、AI 初筛、LUI、同步等任务型页面。

- 顶部是任务上下文，不做大 Hero。
- 摘要指标使用小型信息块，最多 4 个。
- 主操作靠近任务标题或当前模块。
- 右侧辅助区用于设置、阈值、解释、队列状态。
- 候选人结果优先表格或行式复核，不使用大卡片瀑布流。
- 当顶部过空时，先收紧版心、缩短上下距离、把指标贴近标题，再考虑补纹理。
- 指标块优先使用 2-4 个小型数字卡，数值比标签更醒目，避免一排说明文字。
- 允许在工作台头部加入极淡的工程感纹理：网格、光带、柔和光斑，但不要做成营销头图。
- 工作流摘要优先用一句短关系，例如“导入 → 初筛 → 复核 → 导出”，不要画复杂流程图。
- 默认路径必须先于高级选项出现，低频项应收起或延后。

### 5.3 表单弹窗

- 外层画布使用 `#F8FAFD`。
- 内容分组用白底模块承载。
- 分组标题 16px，说明 12px。
- 输入控件高度 34px 左右。
- 底部操作固定在弹窗 footer，不让用户滚到底再找保存。
- 弹窗顶部可以复用工作台头部语言，但只保留缩小版：一层轻纹理、1-2 个摘要数字块、一句短说明。
- 不要把每个分组都包成同级大白盒；能用分隔线分段时，不再额外起卡。
- 默认表单只展示完成任务所必需的字段，筛选、附加导出、说明类配置默认折叠到“高级选项”。
- 当系统已经有合理默认值时，界面要展示“当前预设摘要”，不要直接把控件摊开逼用户决策。

---

## 6. 组件规范

### 6.1 Button

主按钮：

```text
height: 36px
padding: 0 14px
radius: 6px
background: #0062FF
text: #FFFFFF / 13px / 650
shadow: none
```

次按钮：

```text
background: #FFFFFF
text: #1A1A1A / 13px / 600
border: none 或 #E5E7EB 1px（只在白底上需要边界时）
hover: #F3F4F6
```

危险按钮只在删除、不可逆操作中使用红色。

### 6.2 Input / Textarea / Select

```text
height: 34px（Textarea 按内容）
background: #FFFFFF
radius: 6px
border: none 或 #E5E7EB 1px
placeholder: #6B7280
focus: #0062FF 轻量 ring
```

下拉浮层：白底列表，不再额外套浅灰容器；hover / selected 使用 `#EEF4FF`。

### 6.3 Tab / Filter Chip

```text
height: 34px
radius: 6px
padding: 0 12px
active bg: #EEF4FF
inactive bg: #F9FAFB / #F3F4F6
text: #1A1A1A
count: 12px / 650
```

### 6.4 Card / Panel

普通面板：

```text
background: #FFFFFF
radius: 6px / 8px
padding: 14px / 16px
border: none
shadow: none
```

边界依赖父级浅蓝灰画布、间距和标题区，不依赖厚线条。

高级环境面板：

```text
background: 白色到浅蓝白的弱渐变
radius: 18px / 22px
border: #DCE6F4 / #E0E9F3 轻边界
shadow: 0 18px 44px -34px rgba(15,23,42,0.22)
texture: 顶部光带 + 柔和光斑 + 低对比网格（三选二即可）
```

使用规则：

- 只用于页面头部、导入工作台摘要区、重要弹窗头部，不作为通用卡片默认样式。
- 纹理透明度必须低，优先保证文字和数字可读。
- 面板内部的小数字块可以使用半透明白底、轻边界、内高光，建立精致感。
- 不要同时叠加重阴影、厚边框、强渐变、明显玻璃模糊。

### 6.4.1 Summary Metric

用于工作台头部、导入摘要、自动识别摘要。

```text
结构：标签在上，数字在下
标签：11px / 12px / 弱对比 / 可加轻 tracking
数字：18px 左右 / 600 / 比标签更醒目
容器：12px - 14px 圆角，内边距 12px - 14px
风格：普通项用半透明白底；重点项用极淡蓝底
```

规则：

- 最多 4 个，优先 2-3 个。
- 数值型信息优先，例如数量、进度、默认轮次。
- 指标块服务于扫描，不承载长说明。
- 如果一项信息可以并入标题下的一句摘要，就不要再单独做卡。

### 6.5 List Row

```text
height: 64px / 74px
background: #FFFFFF
padding: 0 16px
radius: 6px
border: none
hover: #F8FAFD
```

列表行应优先展示主字段、状态、更新时间、关键操作，避免过多描述。

### 6.6 Badge / Status

状态胶囊使用浅底 + 深色文字。

| 状态 | 背景 | 文字 |
|---|---:|---:|
| 已发布 / 当前 | `#EEF4FF` | `#0062FF` |
| 草稿 / 普通 | `#F3F4F6` | `#4B5563` |
| 通过 | `#ECFDF5` | `#047857` |
| 待定 | `#FFFBEB` | `#B45309` |
| 淘汰 / 失败 | `#FEF2F2` | `#DC2626` |

### 6.7 Segmented Weight Control

用于筛选模板权重编辑。

```text
结构：单条 100% 横向轨道 + 2 个拖拽控制点 + 3 段颜色
最小段宽：5%
总和：固定 100%
交互：拖拽控制点；方向键 1% 微调；Shift + 方向键 5% 调整
```

三段默认语义：

| 分段 | 色值 | 用途 |
|---|---:|---|
| 简历完整度 | `#2563EB` | 材料完整、基础履历 |
| 技能匹配 | `#10B981` | 技术栈、关键词、岗位匹配 |
| 项目经历 | `#F97316` | 项目深度、业务经验 |

不要再使用三个独立数字输入框编辑同一个 100% 权重组。

---

## 7. 场景规范

### 7.1 简历导入与 AI 初筛工作台

- 顶部使用紧凑工作台头部：批次名称、状态、关键操作、少量指标。
- AI 设置面板应清晰表达模板、阈值、是否自动初筛。
- 流程步骤使用 01-04 的轻量步骤卡，避免流程图式大装饰。
- 结果复核用密集行或表格，减少大卡片堆叠。
- 头部允许使用高级环境面板和低对比纹理，但信息必须仍然像工作台，不像宣传页。
- 指标块默认贴近标题和主叙事，不要在宽容器中松散漂浮。
- 右侧 AI 设置面板和左侧头部语言要统一，不能一边精致一边普通。

### 7.2 筛选模板

- 列表展示权重时使用三张小权重卡或三段式摘要。
- 编辑权重时使用 Segmented Weight Control。
- 模板内容、匹配提示词、推荐规则分区展示，避免一个长表单平铺。

### 7.3 LUI / AI 对话

- 对话内容优先可读性，避免过度卡片化。
- Agent、模型、温度、候选人上下文应靠近输入区。
- 工具调用、工作流产物使用紧凑状态块，不使用大面积彩色装饰。

### 7.4 设置页

- 设置项按功能分组。
- 危险操作独立成区。
- API、Gateway、Agent 配置要保留足够说明，但避免长段落。

---

## 8. 禁止项

- 禁止营销页式大 Hero、渐变标题、全屏宣传块。
- 禁止玻璃拟态作为默认风格。
- 禁止厚侧边彩条作为卡片强调。
- 禁止每个模块都加阴影。
- 禁止同质卡片网格堆满页面。
- 禁止把高频主操作藏进更多菜单。
- 禁止使用不带语义的彩色装饰。
- 禁止为了“高级感”降低信息密度。

---

## 9. 设计 Token 摘要

```json
{
  "ims-accent-cobalt": "#0062FF",
  "ims-accent-cobalt-strong": "#1D4ED8",
  "ims-accent-cobalt-soft": "#EEF4FF",
  "ims-accent-cobalt-muted": "#CFE0FF",
  "ims-text-primary": "#1A1A1A",
  "ims-text-secondary": "#4B5563",
  "ims-text-muted": "#6B7280",
  "ims-border-subtle": "#E5E7EB",
  "ims-border-bluegrey": "#E0E9F3",
  "ims-page-bg": "#F3F6FA",
  "ims-shell-bg": "#F8FAFD",
  "ims-panel-bg": "#FFFFFF",
  "ims-panel-muted": "#F9FAFB",
  "ims-success": "#10B981",
  "ims-warning": "#F59E0B",
  "ims-experience": "#F97316",
  "ims-danger": "#EF4444"
}
```
